name: MLFlow Optuna

docker_env:
   image: mlflow_optuna:1

entry_points:
  train:
    parameters:
      name: {type: str, default: "HP-Optuna"}
      epochs: {type: int, default: 3}
      trials: {type: int, default: 2}
    command: "python src/train.py {name} {epochs} {trials}"